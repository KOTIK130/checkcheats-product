<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CheckCheats - Система проверки игроков</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">CheckCheats</a>
            <ul class="nav-menu">
                <li><a href="/" class="nav-link active">Главная</a></li>
                <li><a href="/products" class="nav-link">Продукты</a></li>
                <% if (user) { %>
                    <li><a href="/dashboard" class="nav-link">Личный кабинет</a></li>
                    <% if (user.group === 'Admin') { %>
                        <li><a href="/admin" class="nav-link">Админ панель</a></li>
                    <% } %>
                    <li><a href="#" onclick="logout()" class="nav-link">Выйти</a></li>
                <% } else { %>
                    <li><a href="/login" class="nav-link">Войти</a></li>
                    <li><a href="/register" class="btn-primary">Регистрация</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

    <main class="container">
        <section class="hero">
            <h1>CheckCheats - Система проверки читов</h1>
            <p>Профессиональная система для проверки игроков на использование читов. Защитите честную игру!</p>
            <div style="display: flex; gap: 1rem; justify-content: center; margin-top: 2rem;">
                <a href="/products" class="btn-primary">Смотреть продукты</a>
                <% if (!user) { %>
                    <a href="/register" class="btn btn-secondary">Создать аккаунт</a>
                <% } %>
            </div>
        </section>

        <section class="features" style="margin-top: 6rem;">
            <h2 style="text-align: center; font-size: 2.5rem; margin-bottom: 3rem;">Почему выбирают нас</h2>
            <div class="dashboard-grid">
                <div class="card">
                    <h3 style="color: var(--primary-color); margin-bottom: 1rem;">🔍 Точное обнаружение</h3>
                    <p style="color: var(--text-secondary);">Мгновенная проверка процессов и файлов на наличие читов с точностью 99.9%</p>
                </div>
                <div class="card">
                    <h3 style="color: var(--primary-color); margin-bottom: 1rem;">📊 Детальные отчёты</h3>
                    <p style="color: var(--text-secondary);">Полная информация о подозрительных процессах и программах игрока</p>
                </div>
                <div class="card">
                    <h3 style="color: var(--primary-color); margin-bottom: 1rem;">🚀 Быстрая проверка</h3>
                    <p style="color: var(--text-secondary);">Сканирование системы за считанные секунды без нагрузки на ПК</p>
                </div>
                <div class="card">
                    <h3 style="color: var(--primary-color); margin-bottom: 1rem;">🔒 Конфиденциальность</h3>
                    <p style="color: var(--text-secondary);">Безопасная проверка без доступа к личным данным пользователя</p>
                </div>
            </div>
        </section>

        <section style="margin-top: 6rem; text-align: center;">
            <h2 style="font-size: 2rem; margin-bottom: 1rem;">Готовы начать?</h2>
            <p style="color: var(--text-secondary); margin-bottom: 2rem;">Присоединяйтесь к тысячам довольных пользователей</p>
            <a href="/register" class="btn-primary" style="font-size: 1.125rem; padding: 1rem 2rem;">Создать аккаунт бесплатно</a>
        </section>
    </main>

    <script>
        function logout() {
            fetch('/api/auth/logout', { method: 'POST' })
                .then(res => res.json())
                .then(data => {
                    if (data.success) {
                        window.location.href = data.redirect;
                    }
                });
        }
    </script>
</body>
</html>